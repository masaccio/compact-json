{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Tests,
            "type": "python",
            "request": "test",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {"PYTEST_ADDOPTS": "--no-cov"},
            "justMyCode": true
        }
    ]
}